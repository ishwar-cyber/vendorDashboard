import { CheckTimeMorePipe } from './check-time-more.pipe';

describe('CheckTimeMorePipe', () => {
  it('create an instance', () => {
    const pipe = new CheckTimeMorePipe();
    expect(pipe).toBeTruthy();
  });
});
